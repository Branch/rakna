import { ICatCard } from './CatCard'

const base: ICatCard = {
    title: 'Hello world!',
    body: 'This is an example! What a nice cat card, wow. Only missing an image!',
    author: 'Andreas Beuger',
}

export const mockCatCardProps = {
    base,
}
